# MongoDB connection string (used inside Docker)
MONGO_URI=mongodb://mongo:27017/bookdb

# Secret key for JWT token signing
JWT_SECRET=supersecret

# Optional: Port (your Express server will use this if set up for it)
PORT=34723
